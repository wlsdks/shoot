server:
  port: 8100

spring:
  data:
    mongodb:
      uri: mongodb://admin:admin123@localhost:27017/shoot
      database: shoot  # 원하는 데이터베이스 이름

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

# 로깅 설정
logging:
  level:
    root: INFO
    com.example: DEBUG
    org.springframework.data.mongodb: DEBUG